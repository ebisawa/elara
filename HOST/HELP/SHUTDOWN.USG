shutdown - システム停止

usage: shutdown [option] [returncode]
option:
    -q  確認なしで実行
    -s  なにも表示しない(確認もしない)
    -f  ログイン中のユーザーがいても強行する

    システムを停止します。

    何もオプションがない場合、ログイン中のユーザーがいなくなるまで
    常駐して監視します。その際、空いたチャンネルから着信を停止します。

    elara.exe はリターンコードとして returncode を返します。
    省略すると 0 になります。

